[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
lib_deps =
  bblanchon/ArduinoJson@^6.19.4
  miguelbalboa/MFRC522@^1.4.2
build_flags =
  ; If you include custom MFRC522v2 driver as a git dependency, specify here

; Example to add a git dependency (uncomment and replace URL if needed)
; lib_deps =
;  https://github.com/<user>/<mfrc522-v2-repo>.git
